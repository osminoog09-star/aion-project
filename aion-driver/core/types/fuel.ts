/** Расширяемые типы топлива для платформы (см. roadmap в docs/PLATFORM.md). */
export type FuelCategory =
  | "petrol"
  | "diesel"
  | "lpg"
  | "cng"
  | "ev";

export type TransmissionKind = "manual" | "automatic" | "cvt" | "dct";
