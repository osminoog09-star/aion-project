; Production-safe defaults for AION Driver (do not commit tokens here).
registry=https://registry.npmjs.org/
strict-ssl=true
; Не задавайте здесь npm node-options с Windows-only флагом доверия к CA — иначе сломается EAS Build (Linux).