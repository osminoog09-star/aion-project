/** Shared with foreground watch and headless shift location task — keep in sync. */
export const MAX_ACCURACY_METERS = 45;
export const MIN_STEP_METERS = 15;

/** Android FGS TaskManager sampling — align with headless merge filters. */
export const BACKGROUND_DISTANCE_INTERVAL_M = 75;
export const BACKGROUND_TIME_INTERVAL_MS = 60_000;
