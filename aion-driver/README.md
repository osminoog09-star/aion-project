# AION Driver (module)

Expo mobile app — **module inside AION project** (`aion-com` / `osminoog09-star/aion-project`).

```bash
# From project root
cd aion-driver
npm install
npm run build:manifest
```

CI: GitHub Actions **EAS Build Driver (preview)** at repo root `.github/workflows/`.

Do not maintain a separate sibling folder `Проекты/aion-driver` for releases.
